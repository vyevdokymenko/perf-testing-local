entryPoints:
  web:
    address: ":8080"
    # http:
    #   redirections:
    #     entryPoint:
    #       to: "websecure"
    #       scheme: "https"
  #websecure:
  #  address: ":443"

api:
  dashboard: false
  insecure: false

providers:
  file:
    filename: /etc/traefik/dynamic.yaml
    watch: true

# certificatesResolvers:
#   letsencrypt:
#     acme:
#       email: info@dev.registry.eua.gov.ua
#       storage: /etc/traefik/acme.json
#       httpChallenge:
#         entryPoint: web

log:
  level: INFO
# filePath: "/var/log/traefik.log"

accessLog: {}
